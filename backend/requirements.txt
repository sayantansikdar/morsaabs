# Runtime dependencies. Everything here ships in the Lambda package, so keep it
# lean -- dev/test tooling belongs in requirements-dev.txt.
fastapi==0.110.1
mangum>=0.17.0
pydantic>=2.6.4
python-dotenv>=1.0.1
boto3>=1.34.129
uvicorn==0.25.0
